.DS_Store
Thumbs.db
*.swp
*.swo
*~
__pycache__/
*.py[co]
.pytest_cache/

# NOT ignored, deliberately: plugins/*/skills/ and plugins/*/wheels/.
# They are generated by `make -C pantheon-cli customer-plugin` AND committed — a
# customer clones this repo, so the plugin must be self-contained. See CLAUDE.md.
